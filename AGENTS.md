# Antes de tocar em qualquer arquivo deste repositório

Leia por completo, nesta ordem, os arquivos abaixo:

0. `G:\Meu Drive\PROJETOS\_prompt-mestre\PROMPT_MESTRE_ORQUESTRACAO.md` — protocolo
   de orquestração universal (Fase 0-7, gates, Modo CI Remoto, ledger). Aplica-se a
   todo o trabalho neste e em qualquer outro projeto do usuário. Leia primeiro,
   antes de qualquer arquivo específico deste repositório.
1. `NÃO COMMITAR.md` — guia geral deste projeto: diretrizes de comportamento,
   arquitetura, zonas proibidas (resumo), armadilhas conhecidas (CI, MIUI,
   MediaCodec, etc). Se não existir, crie um.
2. O `PLANO_MELHORIAS.md` mais recente na raiz — plano ativo, zonas proibidas
   completas, protocolo obrigatório por tarefa, estado atual verificado do app. Se não existir, crie um.
3. O `PLANO_MELHORIAS_PROGRESSO.md` correspondente — histórico de tudo que já
   foi feito. Se não existir, crie um.

Não pule esses arquivos achando que já sabe o contexto — eles são atualizados com
frequência e carregam decisões e descobertas que não estão em nenhum outro lugar.